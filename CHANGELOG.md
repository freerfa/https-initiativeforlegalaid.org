# Changelog (append-only, newest at bottom)

2026-09-14 — Security hardening: CSRFProtect + csrf_token in all POST forms; secure uploads (extension allowlist, filetype magic-byte check, secure_filename, collision counter). Files: app.py, all templates. Verify: POST without token 400; fake .jpg rejected.
2026-09-14 — Storage migration: load/save_data moved from site_data.json flat file to site.db SQLite single JSON row. Files: app.py, site.db. Verify: homepage loads from DB, concurrent edits safe.
2026-09-14 — Env pinning: created .venv installs + requirements.txt (Flask 3.1.3, Flask-WTF 1.2.2, filetype 1.2.0, ...). Files: requirements.txt. Verify: python -m pip show Flask OK.
2026-09-14 — Unified login + persistent sessions: one templates/login.html theme for /login, /admin/login, /user/login; find_account role routing; session.permanent 30 days; role-aware nav; remember-row CSS. Files: app.py, templates/login.html, templates/base.html, static/style.css. Verify: admin login 302 to /admin 200; bad creds show Invalid login details.
2026-09-14 — Docs: added PROJECT_HANDOFF.md (this handoff) + CHANGELOG.md with issues log and update protocol. Verify: files exist, compile clean.
2026-09-14 — Review fixes P0+P1: no default creds (ADMIN_BOOTSTRAP=1 one-shot bootstrap, env never overrides stored admins; prod refuses weak SECRET_KEY; load_dotenv added); .gitignore added (.env/site.db/uploads ignored); README rewritten (correct path, PORT=5001, bootstrap + gunicorn recipe, no plaintext creds); removed Flask-SQLAlchemy/SQLAlchemy dead deps; 404+413 handlers; remember-me checkbox now honoured (unchecked = browser-session cookie); single-role session documented; SEO/alt/footer cleanup (meta description + OG tags, descriptive alts, lazy gallery, single Log in footer link). Files: app.py, .gitignore, .env.example, README.md, requirements.txt, templates/base.html, templates/index.html. Verify: compile OK; home/login/register 200; /admin + /account 302 to /login?next=...; login POST without CSRF 400; admin login 302 to /admin then 200; meta description present.
2026-09-14 — Admin account set to Free: replaced default admin/admin123 in site.db with Free (pbkdf2:sha256 hash of Contact12). Verify: login as Free redirects 302 to /admin, dashboard 200.
2026-09-14 — Restored admin/admin123 alongside Free: site.db admin_users now [Free, admin]. Verify: login as admin redirects 302 to /admin, dashboard 200.
2026-09-14 — Responsive overhaul (auto-fit all screens): added hamburger nav toggle (base.html, inline no-JS toggle), breakpoints 1024/768/480/360 + 1400px large-desktop rule, fluid clamp() hero/gallery images, 2-col grids on tablets collapsing to 1-col on phones, stacked admin sidebar/forms/auth/account/footer, 44px tap targets, 16px inputs (no iOS zoom). Files: templates/base.html, static/style.css. Verify: nav-toggle + main-nav in home HTML; all 5 breakpoints in served CSS; home 200.
2026-09-14 — Pushed to GitHub + packaged for sharing: committed 26 files (2308+/212-), pushed main (d0f0e26) + tag v1.0.0 to origin; added Dockerfile, docker-compose.yml, Procfile, pyproject.toml, requirements-prod.txt, .dockerignore; built /tmp/ila-share.zip (37K) + /tmp/ila-share.tar.gz (29K) from v1.0.0. Verify: git log shows d0f0e26 on origin/main; archives list 30 files incl. packaging + docs.
2026-09-14 — Option A deploy kit: deploy/deploy.sh (Ubuntu 24.04, 7 steps, bash -n clean) + deploy/ila.service + deploy/nginx-ila.conf + deploy/MIGRATION_CHECKLIST.md (WP 6 services/4 projects vs Flask 4/3 gaps) + deploy/DNS_CUTOVER.md (23.153.104.141 cutover + rollback). Verify: deploy.sh syntax OK, app compiles.
2026-09-14 — Zero-fuss hosting path: added render.yaml (Render Blueprint: gunicorn web + SECRET_KEY generate + disk) so no VPS/SSH needed. Content parity done: site_data.json + site.db now 6 services / 4 projects (added Civic and Legal Education, Research and Advocacy, Research Projects from live WP). Verify: homepage shows all three new titles, home 200.
2026-09-14 — Render readiness fix: DATA_DIR support (DB on persistent disk, survives redeploys) + ensure_db() auto-seed from site_data.json on first boot; DATA_DIR added to render.yaml envVars. Verify: fresh DATA_DIR seeds 6 services/4 projects; local home 200.
2026-09-15 — Render deploy fix: gunicorn==23.0.0 added to requirements.txt (was only in requirements-prod.txt, so build `pip install -r requirements.txt` never installed it → "gunicorn: command not found", exit 127). Files: requirements.txt. Verify: Render build installs gunicorn-23.0.0; start command boots 2 workers.
2026-09-15 — PRODUCTION LIVE on Render: https://https-initiativeforlegalaid-org-1.onrender.com (service srv-dakep4bm8hqs73ebt5t0, branch main @ d29c596, start `gunicorn -w 2 -b 0.0.0.0:$PORT app:app`, 2 workers, health 200). Verified externally: home 200 with all 6 services + 4 projects seeded; /login 200 CSRF; /admin 302→login guard; custom 404 works. Remaining before domain cutover: verify admin login (Free), set ADMIN_BOOTSTRAP=0, confirm persistent disk at /opt/render/project/src/data, add custom domain + DNS records, consider paid plan (free sleeps ~15 min idle).
2026-09-15 — Fixed missing photos on Render: seed images (hero, hero-alt, 6 gallery, 3 testimonials) lived only in local static/uploads/ which was git-ignored → fresh Render checkout served 404s. Fix: git add -f the 11 seed jpgs (~700KB) + .gitignore whitelist (!static/uploads/{hero,hero-alt,gallery-*,testimonial-*}.jpg) so future user uploads stay ignored. Files: .gitignore, static/uploads/*.jpg. Verify: all 10 image URLs 200 with correct sizes on https://https-initiativeforlegalaid-org-p51u.onrender.com. Note: NEW images uploaded via admin on Render persist only while the instance lives — move uploads under DATA_DIR disk if persistence needed.
2026-09-15 - Fix: cache-busted style.css (?v=20260915b) so stale cached CSS cannot show an unstyled hamburger button; moved nav toggle from inline onclick to addEventListener script (more robust, CSP/extension safe).
2026-09-15 - Removed hamburger nav-toggle button entirely per user request; nav links now always visible at all screen sizes (mobile nav wraps/centers). CSS cache-bust bumped to v=20260915c.
2026-09-15 - Re-added nav-toggle hamburger button, now shown ONLY on screens <=768px (hidden on desktop); menu opens/closes via JS with aria-expanded; CSS cache-bust v=20260915d.
2026-09-15 - Incorporated two companion sites (paytack.co.ke/ila + /ila/partnerships) into this website: added /leadership page (team with photos, organogram, impact indicators, knowledge centre, accountability policies), /partnerships page (UNFPA + CESYU partner cards, ceremony artwork, 26 institution categories, 10 partner-value cards, donor due-diligence), 17 photos downloaded to static/uploads (gitignored intentionally), nav + footer updated with real social links (Facebook/LinkedIn/X @LegalAid4SS), contact email set to info@initiativeforlegalaid.org, Access to Justice service description enriched.
2026-09-15 - INCIDENT: site down ~1h after photos deploy; cause = service URL accidentally entered in Render Root Directory field (runner cd into nonexistent path, restart loop). Fix: cleared Root Directory in Settings + redeploy (user action). Post-fix verification: all pages 200 (/ /partnerships /leadership /login /register), /admin 302 guard OK, all 23 images 200, no placeholders remaining.
2026-09-15 - Partnerships page: CESYU ceremony card reworded from upcoming to dated (15 September 2026). DNS_CUTOVER.md: added critical mail-preservation warning (MX/mail A/SPF must stay on old server during website cutover; only @ and www records change). Verified MX for domain exists, so info@initiativeforlegalaid.org just needs its mailbox created in OraWebHost cPanel.
2026-09-15 - Docs overhaul: PROJECT_HANDOFF.md refreshed — status line now p51u URL + domain-not-cut-over note; section 1 lists /leadership + /partnerships; section 2 updated (Render auto-deploy, Root Directory must stay empty, mail/mailbox facts, working tree clean); repo map rebuilt (new templates, deploy/, render.yaml, packaging files, uploads ephemerality note); routes list includes both new pages; issues log extended #9-13 (hamburger cache-bust saga, photos 404, Root Directory outage, domain typo, mail-breakage prevention); production checklist re-scored (9 done / 10 open incl. DNS cutover, mailboxes, credential rotation, paid plan, upload persistence).
2026-09-15 - Classic redesign: appended CLASSIC THEME OVERRIDE layer to style.css (serif Georgia headings + brand mark with gold left rule; deep navy #16324f + antique gold #a8823c + cream #faf8f3 palette; all pills/rounded corners squared to 2px; uppercase letterspaced buttons with flat hover; solid white topbar with navy rule; hero on cream paper bg; cards with crisp 1px borders + gold top accent; solid navy CTA banner and footer with gold rules; squared form fields). Cache-bust v=20260915d -> v=20260915e. Files: static/style.css, templates/base.html. Verify: local home 200, served CSS contains override block, page references v=20260915e.
