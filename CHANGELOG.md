# Changelog (append-only, newest at bottom)

2026-09-14 — Security hardening: CSRFProtect + csrf_token in all POST forms; secure uploads (extension allowlist, filetype magic-byte check, secure_filename, collision counter). Files: app.py, all templates. Verify: POST without token 400; fake .jpg rejected.
2026-09-14 — Storage migration: load/save_data moved from site_data.json flat file to site.db SQLite single JSON row. Files: app.py, site.db. Verify: homepage loads from DB, concurrent edits safe.
2026-09-14 — Env pinning: created .venv installs + requirements.txt (Flask 3.1.3, Flask-WTF 1.2.2, filetype 1.2.0, ...). Files: requirements.txt. Verify: python -m pip show Flask OK.
2026-09-14 — Unified login + persistent sessions: one templates/login.html theme for /login, /admin/login, /user/login; find_account role routing; session.permanent 30 days; role-aware nav; remember-row CSS. Files: app.py, templates/login.html, templates/base.html, static/style.css. Verify: admin login 302 to /admin 200; bad creds show Invalid login details.
2026-09-14 — Docs: added PROJECT_HANDOFF.md (this handoff) + CHANGELOG.md with issues log and update protocol. Verify: files exist, compile clean.
2026-09-14 — Review fixes P0+P1: no default creds (ADMIN_BOOTSTRAP=1 one-shot bootstrap, env never overrides stored admins; prod refuses weak SECRET_KEY; load_dotenv added); .gitignore added (.env/site.db/uploads ignored); README rewritten (correct path, PORT=5001, bootstrap + gunicorn recipe, no plaintext creds); removed Flask-SQLAlchemy/SQLAlchemy dead deps; 404+413 handlers; remember-me checkbox now honoured (unchecked = browser-session cookie); single-role session documented; SEO/alt/footer cleanup (meta description + OG tags, descriptive alts, lazy gallery, single Log in footer link). Files: app.py, .gitignore, .env.example, README.md, requirements.txt, templates/base.html, templates/index.html. Verify: compile OK; home/login/register 200; /admin + /account 302 to /login?next=...; login POST without CSRF 400; admin login 302 to /admin then 200; meta description present.
2026-09-14 — Admin account set to Free: replaced default admin/admin123 in site.db with Free (pbkdf2:sha256 hash of Contact12). Verify: login as Free redirects 302 to /admin, dashboard 200.
2026-09-14 — Restored admin/admin123 alongside Free: site.db admin_users now [Free, admin]. Verify: login as admin redirects 302 to /admin, dashboard 200.
2026-09-14 — Responsive overhaul (auto-fit all screens): added hamburger nav toggle (base.html, inline no-JS toggle), breakpoints 1024/768/480/360 + 1400px large-desktop rule, fluid clamp() hero/gallery images, 2-col grids on tablets collapsing to 1-col on phones, stacked admin sidebar/forms/auth/account/footer, 44px tap targets, 16px inputs (no iOS zoom). Files: templates/base.html, static/style.css. Verify: nav-toggle + main-nav in home HTML; all 5 breakpoints in served CSS; home 200.
2026-09-14 — Pushed to GitHub + packaged for sharing: committed 26 files (2308+/212-), pushed main (d0f0e26) + tag v1.0.0 to origin; added Dockerfile, docker-compose.yml, Procfile, pyproject.toml, requirements-prod.txt, .dockerignore; built /tmp/ila-share.zip (37K) + /tmp/ila-share.tar.gz (29K) from v1.0.0. Verify: git log shows d0f0e26 on origin/main; archives list 30 files incl. packaging + docs.
2026-09-14 — Option A deploy kit: deploy/deploy.sh (Ubuntu 24.04, 7 steps, bash -n clean) + deploy/ila.service + deploy/nginx-ila.conf + deploy/MIGRATION_CHECKLIST.md (WP 6 services/4 projects vs Flask 4/3 gaps) + deploy/DNS_CUTOVER.md (23.153.104.141 cutover + rollback). Verify: deploy.sh syntax OK, app compiles.
2026-09-14 — Zero-fuss hosting path: added render.yaml (Render Blueprint: gunicorn web + SECRET_KEY generate + disk) so no VPS/SSH needed. Content parity done: site_data.json + site.db now 6 services / 4 projects (added Civic and Legal Education, Research and Advocacy, Research Projects from live WP). Verify: homepage shows all three new titles, home 200.
2026-09-14 — Render readiness fix: DATA_DIR support (DB on persistent disk, survives redeploys) + ensure_db() auto-seed from site_data.json on first boot; DATA_DIR added to render.yaml envVars. Verify: fresh DATA_DIR seeds 6 services/4 projects; local home 200.
