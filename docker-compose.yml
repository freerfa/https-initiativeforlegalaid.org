services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      PORT: 8000
      FLASK_ENV: production
    volumes:
      - site_data:/app/data
      - ./static/uploads:/app/static/uploads
    restart: unless-stopped

volumes:
  site_data:
